import { ITheme } from './theme.type'

import variable from '../variable'

const theme: ITheme = {
  color: {
    card: variable.color.white,
    icon: variable.color.blue800,
    text: variable.color.blue800,
    background: variable.color.blue100
  }
}

export default theme
